
/*let titulo=document.getElementById('encabezado');
//console.log(titulo.innerHTML);

titulo.style.color="blue";
let concepto=document.getElementById("concepto");
//console.log(concepto.innerText);


let parrafo=document.createElement("p");//crea un elemento p
let texto=document.createTextNode("aqui va el texto");//el texto que se agrega
let datanuevo= document.getElementById("datos");//guardo en variable el texto

datanuevo.appendChild(parrafo);//appendChild agrega el elemento p
parrafo.appendChild(texto);//al elemento le agrego el texto

function valida() {
	let nombre = document.getElementById("nombre").value; //llamo el name
	let radios = document.getElementByName("programar"); //lo que marco
	let estudia = document.getElementById("estudiante"); //llamo su respuesta
	var valorradio = "";

	for (var i = 0; i < radios.length; i++) { //para poder sacar el resultado de radios
		if (radios[i].checked) { //checked verifica si esta seleccionado
			valorradio = radios[i].value;
		}
	}
	if (estudia.checked) {
		estudia = "SI";

	} else {
		estudia "NO";
	}
	console.log(nombre);
}  */

